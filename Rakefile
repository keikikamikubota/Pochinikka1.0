# Add your own users in files placed in lib/users ending in .rake,
# for example lib/users/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks